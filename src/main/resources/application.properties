# -------------------------------------------------------------------
# Database Configuration (Optimized for Deployment)
# -------------------------------------------------------------------
# Railway provides a variable called MYSQL_URL or DATABASE_URL. 
# We add "jdbc:" prefix if needed.
# Database Configuration
spring.datasource.url=jdbc:mysql://roundhouse.proxy.rlwy.net:3306/railway
spring.datasource.username=root
spring.datasource.password=WEVtGjoeCEkGHMVXNiPoMCEjwYGFJYws
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# -------------------------------------------------------------------
# Server Configuration
# -------------------------------------------------------------------
# Cloud providers assign a random port. This line tells Spring to use it.
server.port=${PORT:8082}

# -------------------------------------------------------------------
# Hibernate / JPA Configuration
# -------------------------------------------------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect